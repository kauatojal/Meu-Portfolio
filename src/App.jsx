import { useEffect, useState } from 'react';
import { translations } from './data/portfolio';
import { Navbar, Hero, Process, About, Projects, Skills, Contact, ProjectModal } from './components/Sections';
import './styles.css';
export default function App(){const [theme,setTheme]=useState(localStorage.getItem('portfolio-theme')||'dark');const [lang,setLang]=useState(localStorage.getItem('portfolio-lang')||'pt');const [project,setProject]=useState(null);useEffect(()=>{document.body.dataset.theme=theme;localStorage.setItem('portfolio-theme',theme)},[theme]);useEffect(()=>localStorage.setItem('portfolio-lang',lang),[lang]);useEffect(()=>{const close=e=>e.key==='Escape'&&setProject(null);window.addEventListener('keydown',close);return()=>window.removeEventListener('keydown',close)},[]);useEffect(()=>{const orbit=document.querySelector('.orbit');if(!orbit||window.matchMedia('(prefers-reduced-motion: reduce)').matches)return;let frame=0;const onScroll=()=>{if(frame)return;frame=requestAnimationFrame(()=>{const y=Math.min(window.scrollY,1200);orbit.style.transform=`translate3d(${Math.min(y*.035,42)}px,${Math.min(y*.2,220)}px,0) rotate(-18deg)`;orbit.style.opacity=String(Math.max(.35,1-y/1600));frame=0})};window.addEventListener('scroll',onScroll,{passive:true});return()=>{window.removeEventListener('scroll',onScroll);cancelAnimationFrame(frame)}},[]);const t=translations[lang];return <div className="app"><Navbar {...{lang,setLang,theme,setTheme}}/><main><Hero t={t}/><Process/><About/><Projects onOpen={setProject}/><Skills/><Contact/></main><footer>© {new Date().getFullYear()} Kauã Tojal · Desenvolvido com React, Vite e JavaScript.</footer><a className="top" href="#home" aria-label="Voltar ao topo">↑</a><ProjectModal project={project} onClose={()=>setProject(null)}/></div>}
