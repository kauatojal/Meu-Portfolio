import { useEffect, useState } from 'react';
import gsap from 'gsap';
import { ScrollTrigger } from 'gsap/ScrollTrigger';
import { translations } from './data/portfolio';
import { ArrowUpRight } from 'lucide-react';
import { Navbar, Hero, Process, About, Projects, Skills, Contact, ProjectModal } from './components/Sections';
import './styles.css';
import './social-update.css';
import './contact-about-refinement.css';
import './interaction-refinement.css';
import './reference-modal.css';
import './uiverse-adaptations.css';
export default function App(){const [theme,setTheme]=useState(localStorage.getItem('portfolio-theme')||'dark');const [lang,setLang]=useState(localStorage.getItem('portfolio-lang')||'pt');const [project,setProject]=useState(null);useEffect(()=>{document.body.dataset.theme=theme;localStorage.setItem('portfolio-theme',theme)},[theme]);useEffect(()=>localStorage.setItem('portfolio-lang',lang),[lang]);useEffect(()=>{const close=e=>e.key==='Escape'&&setProject(null);window.addEventListener('keydown',close);return()=>window.removeEventListener('keydown',close)},[]);useEffect(()=>{if(window.matchMedia('(prefers-reduced-motion: reduce)').matches)return;gsap.registerPlugin(ScrollTrigger);const ctx=gsap.context(()=>{const orbit=document.querySelector('.orbit');const techOrbit=document.querySelector('.scroll-tech-orbit');const aboutImage=document.querySelector('.about-visual img');if(orbit){gsap.to(orbit,{x:110,y:520,rotation:360,opacity:1,ease:'none',scrollTrigger:{trigger:'.app',start:'top top',end:'bottom bottom',scrub:1}});gsap.to(orbit,{rotation:'+=360',duration:18,repeat:-1,ease:'none'})}if(techOrbit){gsap.to(techOrbit,{y:420,x:-30,rotation:180,ease:'none',scrollTrigger:{trigger:'.app',start:'top top',end:'bottom bottom',scrub:1}});gsap.to(techOrbit,{rotation:'+=360',duration:14,repeat:-1,ease:'none'})}if(aboutImage){gsap.fromTo(aboutImage,{rotationY:-8,opacity:.72},{rotationY:8,opacity:1,ease:'none',scrollTrigger:{trigger:'.about-visual',start:'top 90%',end:'bottom 15%',scrub:1}})}gsap.utils.toArray('.hero-copy,.hero-visual,.marquee,.about-copy,.about-visual,.skills>*,.contact-intro,.contact-form-shell,footer').forEach((el,index)=>{gsap.fromTo(el,{y:28,opacity:0},{y:0,opacity:1,duration:.7,delay:index*.035,ease:'power2.out',scrollTrigger:{trigger:el,start:'top 92%',once:true}})});gsap.utils.toArray('.project-card').forEach((card,index)=>{gsap.fromTo(card,{y:36,opacity:0},{y:0,opacity:1,duration:.7,delay:index*.06,ease:'power2.out',scrollTrigger:{trigger:card,start:'top 88%',once:true}})});gsap.utils.toArray('.process-grid article').forEach((card,index)=>{gsap.fromTo(card,{y:24,opacity:0},{y:0,opacity:1,duration:.55,delay:index*.06,ease:'power2.out',scrollTrigger:{trigger:card,start:'top 90%',once:true}})})});return()=>ctx.revert()},[]);const t=translations[lang];return <div className="app"><Navbar {...{lang,setLang,theme,setTheme}}/><main><Hero t={t}/><Process t={t}/><About t={t}/><Projects t={t} onOpen={setProject}/><Skills t={t}/><Contact t={t}/></main><footer>© {new Date().getFullYear()} Kauã Tojal · {t.footer}</footer><a className="top back-to-top" href="#home" aria-label={t.backToTop}><ArrowUpRight size={17} aria-hidden="true"/></a><ProjectModal t={t} project={project} onClose={()=>setProject(null)}/></div>}
